specVersion: "v1beta"
serviceId: "weatherapp"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "weatherapp-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
